User feedback: 'I want to enter notes myself' - three-state notes. NotesMode enum in suduko-uikit (Off/User/Auto with label()). Game replaces notes_mode:bool with notes:NotesMode plus user_marks:[u16;81]; entry in User mode toggles a user mark (empty cells only), Auto keeps the computed-candidate strike-out semantics, Off places digits; clear_selected clears user marks in User mode; keypad only in Off. Save codec bumps to v2 carrying notes mode + user marks, restore stays v1-back-compatible (old saves -> Off, empty marks). Header Notes button cycles Off/User/Auto. TDD all game-level changes; update help text; bump 0.6.2; rebuild pages; refresh/add screenshots. just check green; push origin main.