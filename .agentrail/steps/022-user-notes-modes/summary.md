Added color-numbers-bg.jpg as the app background (v0.6.3): body::before fixed full-viewport layer, cover-fit, opacity 0.15, z-index -1 - strictly below all UI. JPEG copied into the crate, trunk copy-file'd unhashed to the bundle root so the CSS relative url resolves under /sudoku/. Verified over served bundle (loads, computed style, 0 console errors); menu.png and board-in-play.png recaptured. Workspaces 0.6.3, pages rebuilt and deployed, just check green (47/0). Pushed 203f47a.