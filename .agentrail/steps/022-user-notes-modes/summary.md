Three-state pencil notes (v0.6.2): the header Notes button cycles off -> Notes: mine -> Notes: auto. User mode starts empty and digit presses toggle player marks (legal digits only, empty cells only, Erase clears the cell); Auto mode app-fills computed candidates and keeps the old strike-out typing; Off is plain play with the popup keypad. NotesMode enum lives in suduko-uikit with label/next/from_index; suduko-game stores notes:NotesMode + user_marks:[u16;81] and dispatches entry/clear_selected per mode; save codec v2 carries mode + marks with v1 back-compat and fail-closed parsing; 9 new TDD tests plus legacy tests remapped. Verified live: toggle, persistence across reload/resume, auto fill. All workspaces 0.6.2, pages rebuilt and deployed; README gains notes-user.png. just check green (47/0). Pushed dc89c94.