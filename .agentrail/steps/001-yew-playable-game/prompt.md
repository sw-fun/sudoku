Build the playable game per docs/sagas.md step 1: suduko-ui gains a pure TDD'd state module (selection, set/erase with given-cell protection, wrong-value detection against the stored solution, bad-input counter, both highlight rules: row/col/block peers for an empty selected cell and same-number cells excluding wrong guesses for a valued one, win detection, elapsed seconds) and thin Yew components: menu (five difficulty buttons + stats line), board (grid, pad 1..9 + erase, menu button, bad count, timer, win flash overlay with next-board rotation at the same difficulty using a fresh time-derived seed), document-level keyboard handling (1..9, spacebar, Escape to menu). Add the engine crates as path dependencies. Update scripts/build.sh + serve.sh flow as needed. Run scripts/serve.sh, drive the app with playwright-cli, capture menu and in-play board screenshots into docs/images/, embed both in README. just check green (incl. wasm build), commit by named file, agentrail complete.