PWA + mobile fit from user feedback (2 users report scrolling on phones): make the app an installable PWA (manifest with /sudoku/ scope, relative-path service worker with cache-first assets and network-first navigation, theme/meta tags, 192/512 icons), tighten the mobile UI (smaller row/col legend fonts and --coord, tighter margins/padding/gaps, smaller pad buttons, mobile media-query cell clamp), move column labels below the board, bump all workspaces 0.3.0->0.4.0 with cross-workspace pins and lockfiles, rebuild pages/ via scripts/build-pages.sh so the live footer reads v0.4.0. Fix earlier mistakes: absolute /manifest.json and /sw.js paths break under the /sudoku/ Pages subpath (use relative), referenced icons must exist and be trunk copy-file'd. just check green, named-file staging, push origin main.