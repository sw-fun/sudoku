Made the app an installable PWA and tightened the mobile UI, then repaired a broken first attempt: 724ff72 had hand-edited pages/index.html with absolute /manifest.json and /sw.js paths that 404 under the /sudoku/ Pages subpath, referenced nonexistent icons, and left the stale 0.3.0 hashed assets in pages/. Now: all workspaces at 0.4.0 (footer verified in wasm), manifest scoped to /sudoku/ with 192/512/maskable icons, registration-relative service worker (cache-first assets, network-first navigation, offline shell, old-cache cleanup), relative asset hrefs trunk copy-file'd into the bundle, column labels below the board, smaller legends/margins/pads. pages/ rebuilt via scripts/build-pages.sh. just check green. Note: an earlier complete accidentally used --done; reopened 17 and re-completed without it so 018-yew-persistence-polish remains pending.