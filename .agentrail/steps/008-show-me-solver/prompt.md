Show-me mode: the game solves the current board step by step, teaching each strategy as it goes. (1) suduko-tutor: finders take &Candidates; add candidates_with(shown, eliminations) and find_all_in so a persistent elimination layer can feed discovery (TDD). (2) suduko-game: Game.eliminated Vec<(idx,digit)> subtracted from pencil marks and tutor input; show-me state machine: start_show_me, advance (step walkthrough; at last step apply the annotation's effect - placements via set_value, eliminations extend the layer - then auto-select the next lowest-rung strategy) until is_won (TDD on the Wikipedia easy board: loop advance to victory, eliminations persist into marks/offers). (3) suduko-ui: Show me button; walkthrough gains Auto toggle riding the 1s Tick; placements/eliminations visibly land on the board. Playwright e2e: start show-me on a real board, verify strategies advance and the board fills; docs + README; just check green, named-file commit, agentrail complete, push origin main.