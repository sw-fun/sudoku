Bug: completing a board (including via show me) never raised the win count - stats incremented only in Msg::NextBoard, so winning and clicking Menu or reloading at the overlay lost the tally. Move the increment to the won-transition inside mut_game so every winning path (pad, keyboard, popup keypad, show-me step/tick) tallies exactly once; NextBoard just deals the next board. Bump all workspaces to 0.6.1, rebuild pages. Verify end-to-end via crafted save: resume near-won board, place last digit, stats increment and persist across reload. just check green, push origin main.