v0.7.2: starting a new difficulty from the menu with an in-progress board now opens the abandon confirmation instead of silently discarding it (start_requested queues pending_level; abandon_answered starts the pending level on yes, returns to the live board on no; dialog renders over the menu too). showme::tick gained a TDD'd active flag (inactive ticks freeze clock and solver); persist/abandon_dialog moved to lib.rs holding app.rs at 7 fns. E2E verified all three paths with stats preserved. Pages rebuilt, footer v0.7.2 verified. just check green. Pushed 5350bc8.