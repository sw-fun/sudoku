Two bug fixes: (1) reload with an in-progress game - the timer keeps advancing while sitting on the menu; guard Msg::Tick so elapsed time only counts while the game screen is shown. (2) a wrong placement prunes peer pencil notes and erasing/replacing never restores them - move the peer-prune in set_value behind the correctness check so only correct placements prune (wrong guesses leave notes untouched; erase keeps them; the eventual correct placement prunes legitimately). Update the prune test to use a correct placement, add a wrong-placement-preserves-notes test; verify both live over the served bundle; bump all workspaces to 0.7.1; rebuild pages; just check green; push origin main.