Keyboard erase from user feedback: Backspace and Delete (plus Mac forward-delete) now erase the selected cell alongside the existing spacebar, so keyboard players never touch the Erase button. TDD the pure parse_key (new tests/keys.rs in suduko-ui; keys module made pub for the rlib test target), verify Backspace/Delete/Space in-browser via playwright key presses, update docs/plan.md input bullet, just check green, named-file commit, agentrail complete, push origin main.