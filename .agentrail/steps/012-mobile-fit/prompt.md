Mobile fit from user feedback: (1) board slightly wider than viewport on phones with the last column clipped - add a responsive viewport-fit (rem-based/percentage cell sizing with a max-width container, meta viewport already present; verify at 390x844 via playwright) so the whole board incl. coordinate labels fits; (2) learn/show-me text panels narrower (max-width tied to the game container, text wraps); (3) reorder game-header controls to: Menu, Learn, Show me, difficulty level (level pushed to the right end). CSS/markup only; docs/plan.md note; just check green, named-file commit, agentrail complete, push origin main.