New pure engine crate components/engine/crates/suduko-tutor: annotated strategy finders for teaching. find_all(shown: &[u8;81]) -> Vec<Annotation> returns ALL applicable strategies for the current board in ladder order: naked single, hidden single, pointing, claiming, naked pair, hidden pair, X-Wing (size 2), XY-Wing. Annotation carries strategy kind, digits, pattern cells, involved units (Row/Col/Block refs), effect (Place or Eliminate list), and ordered TeachSteps (per-step highlighted cells + units + focus digits + explanation text). Reuse suduko-techniques(-advanced) Candidate logic; candidates computed from shown values (clues + user entries). TDD: hand-crafted boards per technique asserting geometry and steps; red first, then minimal implementation. Add crate to engine workspace members; pedantic clippy clean; docs/plan.md notes the tutor crate. just check green, named-file commit, agentrail complete, push origin main.