Bug: on win, the solved overlay dialog renders below the last completed cell (.cell.selected has z-index:1, .overlay has none), so the cell digit pokes through the dialog. Fix: raise the overlay stacking (z-index on .overlay) so the dialog is above any board numbers. CSS-only; verify in-browser via playwright (win a board, elementFromPoint over the selected cell must hit the overlay), update docs if stacking is documented, rebuild pages bundle, just check green, named-file commit, agentrail complete, push origin main.