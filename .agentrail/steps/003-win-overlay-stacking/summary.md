Fixed win-overlay stacking: .cell.selected (z-index:1) painted above the z-index-less solved dialog, so the last completed cell poked through; .overlay now carries z-index:5 (above cells, below the github corner). CSS-only, verified in-browser via playwright elementFromPoint over the selected cell before/after, docs/plan.md notes the rule, pages bundle rebuilt, just check + wasm build green.