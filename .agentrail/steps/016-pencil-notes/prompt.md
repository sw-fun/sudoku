Pencil-mark notes from user feedback (2 users): (1) Notes toggle - marks visible during normal play; with notes on, entering a digit toggles that candidate off/on in the selected cell (user removals layer = the existing eliminated set, now user-owned); Erase in notes mode restores the selected cell's computed candidates. (2) Learn walkthroughs gain an Apply button that applies that strategy's eliminations to the marks and refreshes offers, staying in learn mode. (3) Picker gains Apply all (every listed elimination strategy) and Reset marks (clear the layer). Show-me stop no longer wipes the layer (Reset marks is the explicit clear). TDD the pure layer ops (toggle, apply annotation, apply all, reset, per-cell restore) in suduko-game; wire UI (header Notes button, Apply/Apply all/Reset marks); update help overlay text; playwright e2e; docs/plan.md; keep sw-checklist green; just check green, named-file commit, agentrail complete, push origin main.