Pencil-mark notes from 2-user feedback: Notes toggle (marks visible during play; digit entry toggles candidates; erase restores computed marks; never places), Learn walkthrough 'Apply these removals', picker 'Apply all eliminations' + 'Reset marks'; show-me stop preserves the user layer; help overlay documents it all. TDD 5 tests + full playwright e2e (toggle/restore, apply/apply-all/reset verified on live marks). just check + wasm green; pages rebuilt.