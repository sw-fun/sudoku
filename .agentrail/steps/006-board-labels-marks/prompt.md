Teaching legibility fixes from user feedback: (1) add row/column coordinate labels (1-9) around the board so teaching references like r6c1 and 'row 6' are locatable; (2) increase pencil-mark font size (0.52rem -> ~0.7rem) and darken for readability; set .cell box-sizing border-box so label columns align exactly with cells. Presentational change (markup + CSS), verified in-browser via playwright (labels render 1-9, mark computed font-size, alignment); no new pure logic. docs/plan.md note; just check green, named-file commit, agentrail complete, push origin main.