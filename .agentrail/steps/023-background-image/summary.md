Notes UX cleanup (v0.7.0): the 3-state NotesMode cycle became orthogonal controls - a plain pencil toggle (Notes: off/on) for writing notes vs values with notes always visible while solving, automatic pruning of a placed digit's note from all 20 peers via set_value, and a My-notes section in Customize (show/hide checkbox, Fill in all notes, Clear all notes). Save codec v3 with v1/v2 back-compat lives in new save.rs; structure held inside sw-checklist caps by delegation (trial into build, elapsed into showme::tick, help_overlay into lib.rs, abandon_dialog into app.rs, notes_section sub-view). Help rewritten; 3 captures refreshed. E2E verified: toggle, persistence, fill, hide/show, prune. Footer v0.7.0 verified. just check green. Pushed e8172a8.