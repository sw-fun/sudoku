Create the Cargo workspace per docs/plan.md: crates/suduko-engine (pure Rust lib) and crates/suduko-ui (Yew/wasm-bindgen bin placeholder). Add a root justfile with a check recipe chaining cargo fmt --check, cargo clippy --workspace -- -D warnings, cargo test --workspace, sw-markdown-checker on changed docs, and sw-checklist. Add README pointing at docs/plan.md and docs/sagas.md. Write structural tests first (failing): engine crate exposes Board/Cell modules, ui crate builds for the wasm target placeholder. Seed AGENTS.md with the repo rules modeled on ../../sw-ml-study/demo-extensions (agentrail protocol summary, TDD rule, just check gate, direct-main publication, named-file staging). Keep .gitignore narrow (target/, dist/, local tooling). Run just check green, commit by named file, then agentrail complete.