Usability: starting a new difficulty from the menu silently discarded an in-progress game. Msg::Start routes through start_requested: an in-progress board queues pending_level and opens the abandon confirmation instead; abandon_answered starts the pending level on yes (stats kept), returns to the live board on no. showme::tick gains an active flag (TDD: inactive tick freezes clock and solver); persist moves to lib.rs persist_slot to hold app.rs at 7 functions; abandon dialog renders on the menu too. E2E verified all three paths; bump 0.7.2; rebuild pages; just check green; push.