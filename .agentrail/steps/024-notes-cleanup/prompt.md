Notes UX cleanup from user feedback: replace the 3-state NotesMode cycle with (a) a single pencil toggle (header 'Notes' button, on = digit presses write user notes, off = place values), (b) user notes always visible while present (visibility no longer a mode), (c) auto-prune: placing a value removes that digit's note from all 20 peers (also from show-me placements via set_value), (d) 'Fill in all notes' and 'Clear all notes' one-shot actions in the Customize dialog filling/clearing the user-marks layer via NoteOp::FillUser/ClearUser, (e) keypad popup only when pencil off. Save codec v3 (pencil flag + marks) with v2/v1 back-compat (v2 notes=1 -> pencil on; v2 notes=2 -> pencil off + computed-candidate fill; v1 -> defaults). Rewrite user_notes tests; update help text; recapture notes-user/customize/board screenshots; bump all workspaces to 0.7.0; rebuild pages; just check green; push origin main.