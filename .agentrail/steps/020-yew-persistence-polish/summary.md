v0.6.0 feature round: rebrand to Learn/Practice Sudoku (menu, title, manifest, README); abandon-progress confirmation dialog on Menu/Escape for in-progress boards (X/click-outside/Escape close, stats kept); localStorage persistence via TDD'd versioned save codec in suduko-game build.rs (save/restore, fail-closed, solved boards restore stats-only) - fixes the stats-reset-on-reload report; single-slot Resume on the menu with level + elapsed; Customize is now a modal popup (was a details bar) with close X, Escape, click-outside, and Clear stats, which also holds app.rs/board.rs under sw-checklist function caps (view_board and dialogs take link and build their own callbacks); docs/offline-use.md documents phone PWA install (iOS/Android), offline behavior, updates, troubleshooting. All workspaces 0.6.0; pages rebuilt with verified footer provenance. Second commit refreshed all README screen captures via playwright over the served bundle and added customize-dialog.png + abandon-dialog.png (initial capture attempts looked broken because test clicks targeted given-clue cells, which correctly ignore input). just check green both commits (47/0). Pushed c57f5c9 + 5e2c9a9.