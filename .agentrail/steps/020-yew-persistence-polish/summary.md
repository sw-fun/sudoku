Fixed win counts stuck at zero: the increment existed only in Msg::NextBoard so winning via show me and then leaving (Menu/reload at the overlay) never tallied. Moved the tally to the won-transition inside mut_game so every winning path increments exactly once (NextBoard just deals the next board); Tick compacted via u32::from(!g.won) to keep update under the 50-line sw-checklist cap. Verified end-to-end over the served bundle with a codec-crafted one-cell-from-won save: resume, place final 9, overlay appears, stats 0=2->0=3 persisted, and reload shows Easy: 3 with no resume. Workspaces bumped 0.6.0->0.6.1, pages rebuilt (footer v0.6.1 verified). just check green. Pushed 8943c4f.