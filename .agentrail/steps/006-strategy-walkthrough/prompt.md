Learn mode part 2 (animated walkthrough): stepping controller (Next/Back/Close) over the tutor TeachSteps for the selected annotation. Per-step board rendering: pattern cells colored/highlighted, involved rows/cols/blocks tinted, focus digits emphasized in pencil marks, elimination candidates pulse red then strike, placement steps pulse green; explanation text shown under the board; CSS keyframe animations. Playwright end-to-end verification on a real mid-game board (X-Wing or pair): step through, assert cell classes/units per step; README capture + docs/plan.md update; rebuild pages bundle; just check green, named-file commit, agentrail complete, push origin main.