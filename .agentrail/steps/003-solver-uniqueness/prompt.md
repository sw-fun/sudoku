TDD a deterministic backtracking solver with candidate ordering and a solution counter capped at 2. Failing tests first: solves known puzzles, counts exactly one solution for unique puzzles, counts 2+ for ambiguous ones, returns fast (performance sanity with a time bound) on worst-case inputs, and proves a solved board. Use the engine's own generated fixtures where a generator does not exist yet by embedding known puzzle strings. just check green, docs updated, commit, agentrail complete.