When the 9th instance of a number is placed correctly on the board (all 9 of that digit are in correct positions), the input-pad button for that number must be disabled/grayed-out so it can't be selected. Keep the engine pure: TDD a pure helper in suduko-game (or engine crate if more natural) that computes which digits are complete given user values + solution; Yew input pad renders disabled state from it. Add styling for the disabled button in main.scss. Update docs with the behavior. just check green, named-file staging, detailed commit, agentrail complete, push origin main.