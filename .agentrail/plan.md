Build the Yew/WASM playable Sudoku application on the accepted engine (components/engine), replanned into two steps per docs/sagas.md. Step 1 delivers the playable game: menu with five difficulty buttons and stats, board rendering with selection, both highlight rules, red wrong values, bad-input counter, keyboard and button input with erase, elapsed time, win flash with next-board rotation, Escape and menu navigation; pure state transitions TDD'd; trunk bundle served by scripts/serve.sh with basic-http-server on 0.0.0.0:9501; README screen captures via playwright-cli. Step 2 adds versioned cookie persistence, persisted per-level stats, and the final acceptance report. Every step: red/green tests, just check green (per-workspace fmt, pedantic clippy -D warnings, tests, wasm build, docs check, sw-checklist), docs updated with behavior, named-file staging, detailed commit, agentrail complete, push origin main.