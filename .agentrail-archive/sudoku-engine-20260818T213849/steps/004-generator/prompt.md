TDD random puzzle generation: seeded RNG full-grid generation via randomized backtracking (all 81 cells valid), then uniqueness-preserving clue digging (remove a cell only if the capped counter still finds exactly one solution), optional symmetric digging. Failing tests first: full grids are valid and complete; dug puzzles keep exactly one solution; deterministic under a fixed seed; givens count is parameterizable; generation terminates within an attempt bound. just check green, docs updated, commit, agentrail complete.