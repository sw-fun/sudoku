TDD difficulty targeting: define score bands and givens-count ranges for easy, medium, hard, harder, hardest per docs/plan.md (easy = singles only, medium = +locked candidates, hard = +pairs/triples, harder = +X-wing class, hardest = beyond the technique ladder / trial). Implement the accept/reject generation loop: generate, dig, verify uniqueness, grade, accept only inside band and givens range, else re-dig or regenerate under an attempt cap. Failing tests first: each level returns a unique puzzle inside its band; loop gives up gracefully and retries a fresh grid after the cap. just check green, docs updated, commit, agentrail complete.