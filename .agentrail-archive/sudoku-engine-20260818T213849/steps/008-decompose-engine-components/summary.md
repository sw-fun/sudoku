Decomposed the engine into component workspaces per the sw-mlpl pattern: components/engine (grid, solver, generator, techniques, techniques-advanced, grader, engine facade) and components/ui, each its own cargo workspace with shared root target/, serial.sh global build lock, cargo-all.sh driver, build.sh/serve.sh scripts (basic-http-server on 0.0.0.0:9501), and a trunk-built favicon/index.html bundle. Pedantic clippy enabled and satisfied. Port bugs fixed with tests: try_trial depth-0 semantics (hardest 0/30 -> 30/30), grade fails fast on inconsistent boards, hidden_set present-digit enumeration. just check green (sw-checklist 36/0); committed 8d93d6d and pushed.