Restructure per user direction and the sw-mlpl component pattern: replace the single-crate workspace with top-level components, each its own cargo workspace. components/engine holds suduko-grid (Board/Cell/coords/format), suduko-solver (state/search), suduko-generator (rng/full/dig, no Puzzle dependency), suduko-techniques (Candidates/Effect/UNITS + singles/intersections/subsets), suduko-techniques-advanced (xy_wing/fish), suduko-grader (ladder chain-of-responsibility, grade, trial), and suduko-engine as the facade (Puzzle, Level, difficulty bands as config, generate dispatch). components/ui holds suduko-ui. Aim for four modules per crate and four functions per module (hard checklist max 7); split oversized functions. Root .cargo/config.toml shares one target dir; scripts/serial.sh wraps cargo under a global build lock (macOS-portable); scripts/cargo-all.sh runs one cargo command across every workspace sequentially; justfile check chains per-workspace fmt/clippy/test plus the ui wasm build, docs check, and sw-checklist. Move existing integration tests into the crate they exercise and keep the facade API stable where practical. Update AGENTS.md, README, docs/plan.md to describe the component layout. Gate must be green before the commit; agentrail complete.