Add seeded statistical gate tests for difficulty robustness: for each level, sample a fixed number of puzzles with a fixed seed set and assert every sample sits inside its level band, mean scores are strictly monotone easy < medium < hard < harder < hardest, every easy sample is solvable by naked+hidden singles only, no hardest sample is singles-solvable, and every sample has exactly one solution. If gates expose band overlap, tune bands in this step with the evidence recorded in docs. just check green, docs updated with the measured table, commit, agentrail complete.