TDD the grid model in crates/suduko-engine: Cell (given/user value 1..9 or empty), Board of 81 cells with a stored solution, row/column/block index helpers, peer sets. Start with failing tests for: valid/invalid value placement against row+column+block constraints, peer set contents for corner/center cells, compact puzzle/solution string serialization round-trip, and rejection of malformed strings. Implement minimally to green. No solver yet. Run the scoped cargo test then just check, update docs with the model API, commit, agentrail complete.